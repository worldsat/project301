package com.uilover.project301.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand / Primary ──────────────────────────────────────────────────────────
val Primary       = Color(0xFFD32F2F)   // Main brand colour, major CTAs
val OnPrimary     = Color(0xFFFFFFFF)   // Text/Icons on Primary

// ── Secondary / CTA ──────────────────────────────────────────────────────────
val Secondary     = Color(0xFFFFB300)   // Active nav state, highlight actions
val OnSecondary   = Color(0xFF000000)   // Text/Icons on Secondary

// ── Surface & Backgrounds ────────────────────────────────────────────────────
val Surface           = Color(0xFFF9F9F9)   // Default screen background
val OnSurface         = Color(0xFF1C1B1F)   // Primary text / high-emphasis icons
val SurfaceVariant    = Color(0xFFF3F3F3)   // Cards, inputs, subtle containers
val OnSurfaceVariant  = Color(0xFF49454F)   // Secondary text, placeholders

// ── Outline & Dividers ────────────────────────────────────────────────────────
val Outline       = Color(0xFFDADADA)   // Borders, dividers, low-emphasis strokes

// ── Error (Material 3 default kept for now) ───────────────────────────────────
val Error         = Color(0xFFB00020)
val OnError       = Color(0xFFFFFFFF)